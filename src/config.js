// Simple config file for local backend URL. Replace the IP with your machine's LAN IP.
// export const BACKEND_URL = 'http://192.168.1.100:3000';
export const BACKEND_URL = 'https://superposable-alijah-bountifully.ngrok-free.dev';

// Note: change to your machine IP (run `ipconfig getifaddr en0`) or use tunnel.
